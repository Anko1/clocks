$(function () {
	$('.multiple-items').slick({
								   infinite: true,
								   slidesToShow: 3,
								   slidesToScroll: 1,
								   prevArrow: $('#prev'),
								   nextArrow: $('#next'),
								   // variableWidth: true
							   });
});

